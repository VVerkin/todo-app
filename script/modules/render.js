import createElements from './createElements.js';

const {
  createHeader,
} = createElements;

// Основная функция
const renderToDo = () => {
  // Создаем хедер страницы
  const h3 = createHeader();
};
